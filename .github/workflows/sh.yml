name: Basic Commands 

on:
  schedule:
   - cron: "*/2 * * * *"
  
jobs:
    demo-commands:
     runs-on: ubuntu-latest

     steps:
      - name: show current working directory
        run: pwd

    
   
   
