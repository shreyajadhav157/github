name: Run python program

on:
  push:
    branches: 
     - main
     
jobs:
  run-all:
    runs-on: ubuntu-latest

  steps:
     #checkout repository code
   - name: Checkout code
     uses: action/checkout@v4

    #set up python
   - name: set up python
     uses: action/setup-python@v5
  with:
     python-version:'3.10'

    #Run first python script
    - name: Run shreya.py
      run: python shreya.py
